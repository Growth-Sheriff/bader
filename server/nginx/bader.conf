server {
    listen 80;
    server_name _;
    
    # HTTP -> HTTPS yönlendirme (SSL kurulunca aktif edilecek)
    # return 301 https://$host$request_uri;
    
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Timeout ayarları (OCR için uzun)
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }
    
    # Dosya yükleme limiti
    client_max_body_size 50M;
    
    # Health check
    location /health {
        proxy_pass http://127.0.0.1:8000/health;
    }
}

# SSL konfigürasyonu (certbot kurulunca aktif edilecek)
# server {
#     listen 443 ssl http2;
#     server_name bader.example.com;
#     
#     ssl_certificate /etc/letsencrypt/live/bader.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/bader.example.com/privkey.pem;
#     
#     # SSL ayarları
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         # ... diğer proxy ayarları
#     }
# }
